# Chromiator issue tracker

This editable tracker uses stable `THR-NNN` IDs. Check an item only when its acceptance note is verified. Status is `open`, `in-progress`, `blocked`, or `done`; priority is `P0`–`P3`. Completion notes should name the verifying test, artifact, or review and the completion date.

Repository steering policy: newly reported issues are logged here and remain queued; they are not implemented immediately unless the user explicitly requests that issue as the active work.

Unresolved issues are grouped by their primary ownership area and ordered by severity and creator impact: `P0` blocks a core workflow, `P1` materially affects correctness or a primary workflow, `P2` is an important improvement or preventative safeguard, and `P3` is low-impact polish. Cross-cutting dependencies remain explicit in each issue's cross-references.

## Unresolved — UI/UX

- [x] **THR-067** - `done` · P2 · Accessibility - Complete the requested
  keyboard/AT-SPI pass. Mnemonics, F6 region navigation, and concise names are
  implemented; native inspection found Source/Target captions overriding
  site-qualified names, arrow/Export naming gaps, and unconfirmed focus/relation
  readback. Corrected and verified 2026-09-19: unique per-site names, named arrow
  and Export, persistent-input F6/Shift+F6 and mnemonic focus readback, and
  independent UI/UX screenshot review. Raw AT-SPI relation lists remain a GTK
  bridge limitation, not claimed evidence. See
  `docs/ACCESSIBILITY_VERIFICATION_2026-09-19.md` for evidence and limits.

- [x] **THR-068** - `done` · P1 · Color picker - Opening the saturated-blue
  Source picker from the native transition fixture aborts in the OKHSL conversion
  at `src/color.rs:108`. Reproduced during private-Sway accessibility testing;
  corrected 2026-09-19 with fixed-lightness/hue chroma contraction at the blue
  boundary and intermediate samples. Two new regressions, all 105 tests, strict
  Clippy, three release checks, and native blue-picker rendering pass. See
  `docs/ACCESSIBILITY_VERIFICATION_2026-09-19.md` and its preserved stderr log.

- [ ] **THR-069** - `open` · P3 · Accessibility polish - Expose picker Original
  and New swatches as named color samples with dynamic color descriptions;
  include the new F6 navigation guidance in the canvas accessible description
  as well as its tooltip. Nonblocking follow-up from independent UI/UX review;
  no full screen-reader acceptance is claimed by the current private-Sway pass.

- [ ] **THR-998** — `open` · P1 · Thresholds — In the hue mapping editor, the first and last bands should be linked/wrap around so they share the same level.

### P1 — High impact

- [x] **THR-059** — `done` · P1 · Thresholds — Added a per-component **Automatic baseline** action that preserves the current 2–32 Band count while rebuilding RGB/Saturation/Value as equal-width input bands with scalar endpoint levels and Hue as equal circular bands with seam-straddling center levels. Process, locks, Link, Working space, and Hue Origin remain independent; ordinary linked propagation reaches only eligible unlocked peers; locked and already-automatic actions are no-ops. Completed 2026-07-18. Evidence: exact n=2/3/8/32 scalar and circular formula fixtures, authoritative RGB black/white plus HSV Saturation/Value endpoint processing, Hue seam processing at 0° and 30° origins, link/lock/Process/model tests, 127 passing native tests, strict Clippy, optimized build, warning-clean 13/13 GTK audit with a 151-event Threshold inspector scenario covering history/preview/no-op/undo/redo/editor resync, and inspected `threshold-auto-rgb-720.png` plus `threshold-auto-hue-1024.png` artifacts with asserted/actual formula sidecars. Cross-reference THR-006, THR-022, THR-031, and THR-034.

- [x] **THR-999** — `done` · P1 · Voronoi UI — Correct the apparent Source/Target picker reversal in compact site rows. `AdwExpanderRow::add_suffix` prepends individually added suffixes, so Source/Target bindings were already correct but their rendered order was reversed. Each row now adds one explicit, stable **Source → Target** container with persistent labels, then attachment/coverage and lock. Completed 2026-07-18 with native audit clicks on both rendered swatches proving the exact picker title, initial color, and Source-versus-Target commit destination; standard and narrow artifacts were inspected.

- [x] **THR-037** — `done` · P1 · Dialogs — Threshold Mapping and the custom color picker are separate modal transient `AdwWindow`s with their own title bars, Escape/close transaction handling, duplicate guards, stable focus transfer, and top-level screenshot capture. Completed 2026-07-16 after the user manually confirmed under GNOME/Wayland that the dialogs move independently of the parent; automated modal/transient, lifecycle, parent-block, focus, and 8/8 GTK audit evidence remains green.
- [x] **THR-006** — `done` · P1 · Editing — Document-level Undo/Redo now covers every committed recipe and Voronoi sampling mutation, restoring the full Recipe plus selected group/site and expanded detail state. Locked, refused, no-op, selection, comparison, and divider changes add nothing; new edits clear redo; Smooth, Influence, picker sliders/wheel, and marker drag coalesce at interaction boundaries. Undo/Redo cancels armed sampling and schedules exactly one latest-result preview. Save/Save As preserves both stacks and moves a recipe savepoint, so history away from it is dirty and redo back to it is clean. Threshold Mapping and the color picker expose separate visible local histories with modal feedback; Done/Select add exactly one global transaction only when changed, while Cancel adds zero. Completed 2026-07-18 with focused savepoint/coalescing tests, native action-state and modal-history audit coverage, strict Clippy, and 8/8 GTK audit evidence. Cross-reference THR-034.
- [x] **THR-015** — `done` · P1 · Threshold visualization — Put an alpha-aware histogram of the actual source image behind every component's mapping graph so the editor explains both the source distribution and the resulting transformation; an empty abstract graph is not acceptable. Each component owns its own histogram. In RGB, editing Red, Green, or Blue alone shows only that channel's histogram over a black-to-that-channel-color ramp. Editing linked/all RGB channels simultaneously shows one combined map with distinct red, green, and blue histogram overlays in the same plotting area; do not multiply, average, or otherwise collapse them into a distribution that hides channel occupancy. In HSV, inspect the demonstrator's histogram behavior before implementation and retain useful distribution semantics, but correct its misleading colors: Hue density belongs on a circular hue-colored input strip with explicit 0°/360° seam behavior, Saturation belongs on a neutral-to-chroma ramp at a clearly identified representative/reference hue, and Value belongs on a black-to-bright ramp. Keep the source histogram visually distinct from Boundary dividers and from the piecewise Output mapping; for Hue, the output strip must show each cropped source band rotated or flattened to its mapped hue. Preserve direct Boundary/Output handles, exact numeric editing, Process/Bypass and Link semantics, accessible non-color-only cues, and responsive rendering. Histogram/ramp generation must use authoritative source and working-space semantics, run off the GTK main thread or use a bounded cache, update after source/space/component/link changes, and never alter processing results. Acceptance requires known per-channel and combined-RGB distribution fixtures, RGB selection/link transitions, correct HSV ramp-color fixtures including the hue seam, grayscale/transparent inputs, demonstrator comparison captures, standard/narrow layout, keyboard/accessibility evidence, and visual comparison between the displayed mapping and processed output. Completed 2026-07-16. Evidence: a bounded 512-bin alpha-weighted RGB/HSV cache is built from the authoritative full-resolution unsmoothed source in the cancellable background-open job; independent RGB and linked RGB retain distinct distributions, Hue excludes achromatic pixels and rotates around the visible origin/seam, Saturation uses an alpha-times-saturation representative hue, Value uses neutral ramps, and linked S/V remain separate labeled dashed/dotted distributions and strips. Exact alpha/bin/non-zero-seam/weighted-mean/strip/immutability/cancellation fixtures, 111 native tests, strict all-target Clippy, and the 8/8 GTK audit pass. Inspected standard/narrow RGB plus Hue/linked-SV/narrow-Value artifacts, and a matching processed-Result capture. Archive comparison retained useful log-density and additive-RGB ideas while rejecting transparent counting, achromatic Hue pollution, and collapsed S/V. Independent review found no P0/P1 issues, and the remaining clarity/evidence notes were resolved before closeout.
- [x] **THR-051** — `done` · P1 · Color picker — Replaced the OKLab/OKLCH panel with bounded-sRGB OKHSL as the default artist-facing picker while retaining HSV and HSL. The complete OKHSL disk has no gamut holes, picker representation remains independent from Voronoi Matching, Source/Target transactions preserve canonical floats, and latent neutral Hue is stable per model. Completed 2026-07-16 by product decision that OKHSL is the right default for this app; conversion/domain/keyboard/transaction fixtures and GTK evidence cover the result. THR-058 keeps OKHSL processing research parked separately.
- [x] **THR-013** — `done` · P1 · Color picker — Finalized OKHSL, HSV, and HSL as three bounded-sRGB views of one canonical floating-point color. Model switching is non-mutating; all three resolve through the same `f64` encoded-sRGB transfer path, preserve independent latent Hue at neutrals, and reproduce `#509BB4` plus dense round trips without numeric drift. Replaced the fixed 42×42 tile renderer with allocation/scale-keyed device-pixel rasters and a vector-clipped circular edge, eliminating the Minecraft-like HSL/HSV treatment without introducing gaps. Completed 2026-07-16 with focused conversion, model-cycle, domain, device-scale, and transaction tests plus standard/narrow visual evidence. Extended-gamut selection is intentionally outside the finalized product contract; THR-045 and THR-027 record the display/output boundary.
- [x] **THR-052** — `done` · P1 · Color picker rendering — Replaced the fixed 42×42 painted-cell grid with allocation/scale-keyed rasters, full device-pixel HSV/HSL sampling, a bounded accelerated OKHSL raster, and one shared vector circular clip/marker geometry. Boundary colors extend under the antialiased clip so filtered edges cannot acquire black seams. Completed 2026-07-16 after inspecting standard HSV/HSL/OKHSL, narrow HSV, and explicit `GDK_SCALE=2` HSL captures: all are continuous, crisply round, gap-free, and free of Minecraft-like blocks. Device-size/domain tests and the full GTK audit cover regeneration and interaction.
- [ ] **THR-034** — `in-progress` · P1 · Information architecture — Reorganize common finishing controls, method-specific controls, progress, dirty state, errors, and status feedback so the inspector reads as an intentional workflow rather than accumulated panels. Scoped progress 2026-07-15: replaced the scrolling PreferencesPage with compact collapsible Method and active-method sections; Method now owns the engine toggle, Presets, and Voronoi Matching; Smooth moved to the canvas toolbar; Color sites owns remaining sidebar height; Pipeline/profile data moved to the footer's far right; and the main inspector no longer exposes Hue Rotation. Scoped progress 2026-07-16: compacted the Threshold inspector into activatable one-line Link/component summaries with hover and accessible detail, moved Reset under terse disclosure, and made every component row open its exact target; the Link summary opens its linked target when enabled or its Red/Saturation anchor when disabled so Link remains directly reachable. Rebuilt the mapping window graph-first around one Target/Link/icon-Reset row, target-driven histograms, a short hint, precise handles, responsive two-column management tiles, and conditional Copy. Explicit linked targets retain individual inspection while Link is on, use Red/Saturation anchors, and disclose mixed peer state without pretending Process or Lock is shared. Scoped progress 2026-07-18: the main header now has compact, accessible Undo/Redo controls whose sensitivity follows open/edit/undo/redo/save state, while Threshold Mapping and color-picker modals own visible local history controls and feedback instead of sending all history state to the parent footer. Evidence: action-state/savepoint and modal-isolation audit assertions, 8/8 GTK audit, and inspected standard/narrow history artifacts. Scoped HIG milestone 2026-07-18: welcome now hides document-only actions and technical feedback, document Open/Save and Undo/Redo are visibly grouped, Save As lives in a labeled document menu, Export uses ordinary header treatment, the preset/matching rows are explicit, the inspector restores native metrics and spacing, site summaries expose Site/attachment/coverage/Source→Target/lock without a full-width destructive bar, and modal-local history is labeled **Changes in this dialog**. The bottom toolbar shows creator-readable pipeline/profile metadata only for documents. Evidence: focused contextual/preset/focus tests and a warning-clean 9/9 GTK audit including exact 1024×600. Scoped Threshold milestone 2026-07-18: replaced the flat summary launchers with a native **Threshold mappings** hierarchy—persistent Working space, direct future-edits Link, conditional linked editor, and three single-open component cards owning Process, mapping-preserving Bands, Lock, Hue Origin where applicable, and exact component editor actions. Link/Lock remain no-preview document changes; Process is component-local; linked Bands preserve independent Process flags and skip locked peers; all controls resync through document and modal history. Evidence: 124 native tests, a 108-event targeted GTK scenario, warning-clean 13/13 full audit, strict Clippy, and inspected RGB 1180/1024/720, HSV, dark-mode, and exact component-editor artifacts. Scoped Automatic baseline progress 2026-07-18: each component card now owns a lock-aware, accessible one-click baseline action with scalar or circular creator-facing semantics, ordinary Link behavior, and complete document-history resynchronization. Evidence: THR-059 formula fixtures, 127 native tests, a 151-event targeted audit, and inspected RGB 720/Hue 1024 artifacts. Broader document identity, progress/error presentation, and export workflow remain open.
- [ ] **THR-032** — `in-progress` · P1 · Sampling — Make direct Voronoi placement and explicit Source replacement legible. Clicking empty visible artwork immediately creates an unlocked site from that sample, while clicking a fixed-pixel-radius marker selects it; Threshold mode must never create sites. Reattach/Resample remains an explicit armed operation with persistent cursor/canvas/status feedback and an obvious Escape/cancel path. Do not restore separate Add Site or Add Color actions. Scoped progress 2026-07-15: direct empty-artwork creation, marker selection, the Threshold guard, and actionable transparent-area feedback are implemented; complete armed-state cursor/canvas/toolbar feedback remains open.
- [ ] **THR-030** — `open` · P1 · Documents — Replace separate image/project entry points with one creator-facing Open flow, then make document kind, path, save target, and replacement state legible without exposing archive mechanics.
- [ ] **THR-035** — `open` · P1 · Export — Redesign Export around creator goals and downstream handoff, with clear format/precision/gamut consequences and remembered defaults instead of a technical format prompt. Cross-reference THR-027.
- [ ] **THR-036** — `in-progress` · P1 · Workspace — Add direct in-canvas Split manipulation and redesign the narrow workspace so comparison and creative controls do not compete for the same drawer. Scoped progress 2026-07-15: removed the below-canvas slider; Split now has a canvas-drawn divider/grip, 12-pixel drag hit geometry, focused keyboard adjustment, and gesture arbitration that preserves site interaction away from the divider. Scoped HIG milestone 2026-07-18: removed the 640×480 canvas minimum pressure, reduced utility-pane width pressure, and rebuilt the viewer toolbar as a centered comparison overlay with Smoothing aligned independently. Exact 1024×600 is warning-free. At widths through 1100 px, comparison modes and the complete Smoothing label/spin use separate end-aligned rows. Isolated root-relative audits at 720×700 and 1024×600 cover both methods and prove all controls remain inside the window, mutually non-overlapping, and beyond the actual inspector edge (Thresholds 408 px; Voronoi 325 px at 720 and 295 px at 1024). Threshold and picker remain separate movable modal transient windows but now fit within 600 px with scrollable content. Broader drawer/workspace work remains. Cross-reference THR-019 and THR-013.

### P2 — Important improvements

- [ ] **THR-019** — `in-progress` · P2 · Comparison — Direct Split-divider dragging is implemented through THR-054 with a visible canvas grip, bounded hit geometry, keyboard fallback, and no secondary slider. Focus zoom remains an optional improvement to evaluate.
- [ ] **THR-011** — `open` · P2 · Color picker — Evaluate perceptual gamut-boundary caching and richer numeric validation messaging after real-user testing.
- [x] **THR-026** — `done` · P2 · Research — Completed the native picker research and selected an in-tree Rust implementation: OKHSL is the familiar default, HSV/HSL remain alternate views, every plane is continuous over bounded sRGB, model changes share one canonical draft, and neutral Hue memory is model-specific. The final renderer uses device-pixel cached rasters with vector circle geometry; no external picker dependency or additional user-facing model is needed. Completed 2026-07-16 with reference values, dense domain/round-trip fixtures, and the implemented THR-013/051 evidence.
- [ ] **THR-007** — `open` · P2 · UX writing — Replace the technical “Influence” label with creator-facing wording after testing alternatives.
- [ ] **THR-008** — `open` · P2 · UX writing — Review “Voronoi”, “Color group”, and “Add Color” terminology with creative users; preserve precision in help text.

## Unresolved — Engine, color pipeline, and platform

### P1 — High impact

- [x] **THR-045** — `done` · P1 · Display color delivery — Finalized the application boundary: Chromiator supplies a bounded encoded-sRGB GTK preview and delegates monitor-profile selection and final display transformation to the OS, window manager, and compositor. The app will not query EDID/colord, implement display profiling, or claim HDR/wide-gamut presentation. Palette-reduced results that need display-specific grading belong in a purpose-built downstream application. Closed by explicit product decision on 2026-07-16; README and the final PNG/EXR contract state the boundary.
- [ ] **THR-017** — `in-progress` · P1 · Color pipeline — Design, process, and expose alpha quantization and input smoothing while preserving straight-alpha precision and cancellation. Scoped progress 2026-07-15: global Smooth source 0–10 runs a cancellable separable Gaussian before both engines, filtering premultiplied RGB+alpha then restoring straight alpha; the finalized project v5 and preset v2 formats store the shared setting directly. Full export uses source-pixel amount; bounded preview uses preview-pixel amount as a documented interaction-cost tradeoff. Alpha quantization remains open.
- [x] **THR-018** — `done` · P1 · Import/export — Finalized native file support without a browser compatibility layer: project v5 and preset JSON v2 are the only accepted editable formats; legacy browser JSON/PNG recipes are not imported or embedded. Older pre-release versions are rejected with actionable diagnostics and never migrated or rewritten. Completed 2026-07-16 with exact-version, missing-field, unknown-field, strict-archive, and unchanged-incompatible-preset tests.

### P2 — Important improvements

- [ ] **THR-024** — `open` · P2 · Documents — Replace the Threshold dialog's heuristic source context guard with a unique document generation or identity token before document replacement can occur asynchronously while dialogs are open.
- [ ] **THR-023** — `open` · P2 · Reliability — Audit repeated Threshold-dialog open/close cycles for GTK/Rust strong-reference cycles and break any retained dialog/widget/session graph without weakening transaction behavior.
- [ ] **THR-009** — `open` · P2 · Performance — Benchmark THR-046's full-source, bounded-histogram automatic-site initialization on large high-entropy and high-resolution inputs. It already runs off the GTK thread and bounds clustering candidates, but the initial full-source histogram pass is non-preemptible; add cancellation checkpoints or chunked cancellation if it can exceed the interaction budget.
- [x] **THR-027** — `done` · P2 · Color output — Finalized the bounded-sRGB output contract: PNG RGBA8/RGBA16 is encoded and tagged sRGB; OpenEXR RGBA32F is a linear-sRGB high-precision intermediate with no ICC/HDR claim. JPEG and other export formats remain unavailable rather than silently down-converted. Wide-gamut intent, PQ/HLG, Rec.2020, tone mapping, and display-specific grading are outside this palette-reduction app and belong downstream. Completed by explicit product decision and existing PNG/EXR precision/alpha fixtures on 2026-07-16.
- [ ] **THR-021** — `open` · P2 · Evidence — Programmatically compare asserted and actual sidecar semantics and improve detection of blank or semantically incomplete light-colored captures.

### P3 — Parked research

- [ ] **THR-058** — `open` · P3 · Voronoi color matching research — **Perceptual (OKHSL)** processing is abandoned for the current product direction after the corrected HSV cone matched the intended dark-value behavior without adding another creator-facing model. Current parked state: OKHSL matching is absent from the GTK menu while its enum, conversion, processing, CLI fixture path, persistence support, and focused tests remain as dormant code; the OKHSL picker does not select this processing mode. Reconsider the experiment only with a named creative advantage, representative image corpus, explicit endpoint-aware geometry, and comparisons against corrected HSV and Cartesian OKLab. Until that future research is intentionally activated, it must not reappear in the normal Matching workflow. Cross-reference THR-051, THR-056, and THR-057.

## Completed

- [x] **THR-001** — `done` · P1 · Welcome — Responsive native welcome, embedded Spectrum example, and command-driven welcome evidence. Completed 2026-07-13.
- [x] **THR-002** — `done` · P1 · Projects — Distinct image/project routing, suffix enforcement, and save-aware replacement guard. Finalized 2026-07-16 on strict project v5: exactly `manifest.json` plus `source/original`, complete current fields, no unknown archive entries, and no pre-release migration path. Current round-trip, version/missing-field rejection, and embedded-source evidence pass.
- [x] **THR-003** — `done` · P1 · Color picker — Draft HSV/HSL/OKLab picker with precision controls, hex, fixed-scale a/b geometry, gamut-projected pointer/keyboard input, and commit/cancel semantics. Verified by color-plane identity/boundary/marker, keyboard, and transaction tests plus wide/narrow picker evidence; completed 2026-07-13.
- [x] **THR-004** — `done` · P1 · Voronoi — Deterministic alpha-weighted initialization, representative full-resolution sampling, and rare-artifact exclusion. Completed 2026-07-13.
- [x] **THR-005** — `done` · P1 · Evidence — App-owned screenshots with asserted/actual metadata and damage rejection. Completed 2026-07-13.
- [x] **THR-010** — `done` · P2 · Color pipeline — Expose per-component Process/Bypass, variable Bands, and exact Boundary/Output editing for RGB and HSV. Verified with focused state/processing tests and readable standard/narrow GTK artifacts; completed as part of THR-014 on 2026-07-13.
- [x] **THR-012** — `done` · P0 · Color picker — Fix OKLab Lightness becoming unresponsive because each value event reconfigured and rewrote the active GTK Adjustment. Model configuration now runs only when the model changes; active drags perform lightweight draft/output refresh. Verified across five 120-update GTK-main-loop sequences from L=0.05 through 0.95, all completing with successful captures and sub-millisecond worst plane renders; completed 2026-07-13.
- [x] **THR-014** — `done` · P1 · Feature parity — First native Threshold/Voronoi parity slice: method-owned RGB+HSV state, encoded-sRGB float processing, degree-correct circular hue, edit-only Link controls, 2–32 Bands, three Voronoi metrics, v3 projects, exact v2 migration, native component UI, and deterministic evidence. Final review correction standardized HSV degrees at processing boundaries, added known-hue/seam/Voronoi regressions, compacted Link controls, and added explicit atomic Apply for exact fields. Verified by 50 native tests, strict Clippy, release and archive gates, sidecars, and standard/narrow GTK review; completed 2026-07-13.
- [x] **THR-016** — `done` · P1 · Presets — Built-ins now use one stable-ID native Recipe registry: eight Threshold looks (the five exact originals plus Hue Poster, Neon Shadows, and Pastel Bands) and five Voronoi looks (Ink & Paper, Desert Dusk, Blueprint, Arcade Four, and Night Neon). Every entry validates, produces multiple colors on the embedded Spectrum source, uses detached Voronoi sites where applicable, and is labeled `(t)`/`(v)`. Finalized 2026-07-16 without copying built-ins into or modifying the user preset directory; reset and preset-history behavior remains covered.
- [x] **THR-020** — `done` · P2 · Projects — After THR-044, validate unique Voronoi site IDs plus finite Source center, Target color, Influence, optional position/sample metadata, and lock invariants in the current project schema. Completed 2026-07-15. Evidence: v4 load/save validates unique nonzero IDs and order, collision-safe next ID, finite/ranged Source, Target, Influence and normalized optional positions; typed sampling/lock metadata round-trips. Current-schema, malformed and v3-flatten migration tests pass.
- [x] **THR-022** — `done` · P1 · Threshold editor — Boundary and Output editing lives in the complete **Edit Threshold Mapping** workspace with Working space, individual component inspection, Process/Bypass, safe Bands controls, persistent Lock mapping, future-edit Link mappings, direction-named one-shot copy, Reset, a focusable Input → Output transfer plot, and precise normalized/Hue-degree editing. HSV Hue exposes a lock-aware Hue origin; plot axes, precise values, and accessibility descriptions identify Hue values as relative degrees from that visible origin. Every primary row is associated with its explicitly labeled control. Pointer motion is draft-only with one preview on release; every discrete action has local undo/redo; Done creates one creative-history transaction; Cancel/Escape/window close restore the complete opening Threshold state and dirty flag. The inspector is now a synchronized read-only summary plus launcher. Verified by model, migration, history and accessibility regressions, strict Clippy, expanded GTK preview-count/lock/origin/focusability/rollback assertions, and inspected standard/narrow artifacts; expanded 2026-07-15.
- [x] **THR-025** — `done` · P0 · Threshold editor — Removed callback-driven `GtkStringList` replacement from the Component `selected-notify` path and separated stable target membership from render refresh. The exact Hue ↔ linked Saturation & Value GTK signal path completed 40 alternating selections with continuous heartbeat, precise edits, and dialog closure in `threshold-dialog.jsonl`; completed 2026-07-13.
- [x] **THR-028** — `done` · P0 · I/O ownership — Open/save/export now make the document workspace insensitive while a job owns the document and leave footer Cancel usable. A creative modal blocks job start so it cannot cover an unreachable Cancel control; a second job is also rejected without `expect`/panic. The isolated `io-workflow` audit verified dialog-conflict rejection, creative-stack lock, an actual Cancel click, cancellation/finish restoration, unchanged dirty state, and second-job rejection; completed 2026-07-13.
- [x] **THR-029** — `done` · P1 · Stability audit — Added the app-owned widget/signal runner, 100 ms JSONL heartbeat, per-process external watchdog, seven-scenario standard/narrow audit, diagnostic capture, and candid manual-coverage matrix. Verified by `tests/artifacts/audit/summary.json` with 7 pass, 0 fail, and no GTK critical/panic; completed 2026-07-13.
- [x] **THR-031** — `done` · P1 · Thresholds — Bands-count changes are now staged in the complete Threshold Mapping dialog and never rebuild an even mapping implicitly. Add splits the selected Output interval (or the widest interval) at its midpoint and duplicates its Output for pixel-exact preservation; Remove merges the selected Boundary (or the deterministic least-output-error pair), retaining the larger interval's Output with a lower-band tie break. Exact 2–32 entry repeats those safe operations as one local-history and preview action. Dialog-local Ctrl+Z/Ctrl+Shift+Z restores reductions, Cancel restores the complete opening Threshold state, and Done contributes one document-level creative-history transaction. Completed 2026-07-15. Evidence: split exactness, deterministic merge, bounds, linked/locked propagation and migration tests; expanded GTK audit assertions for add/one-preview/local undo/lock/Sync/global undo; inspected standard and narrow dialog captures. Cross-reference THR-006.
- [x] **THR-033** — `done` · P1 · Voronoi UX — Replace the disconnected Color group/Source sample controls with a legible list of independent Voronoi sites. Each site row must present its Source center color, Target color, Influence, lock state, and selection/marker identity as one coherent object, with direct selection feedback and progressive disclosure for precise controls. Completed 2026-07-15. Evidence: independent site rows show labeled Source/Target swatches, attachment, Influence, coverage, lock and stable selection identity; precise editing remains in the selected-site disclosure. Standard/narrow site artifacts, 80 tests, and the 8/8 GTK audit passed independent review.
- [x] **THR-038** — `done` · P1 · Canvas overlays — Render Voronoi sites as independent markers only; remove persistent segments between markers and do not draw Source-to-Target connections. Those lines imply an ordered path or a requirement that the desired Target color exists in the source image, neither of which belongs to the free Target-color model. First reproduce the supplied overlay and identify its current owning path, since the present marker drawing code appears marker-only; do not alter linework that is genuinely part of the source artwork. Also hide all Voronoi site indicators whenever Threshold is the active Method, including Result, Split, and Source comparison views. Switching to Threshold must remove the Voronoi-only overlay immediately without discarding site selection; switching back may restore independent markers. Keep all markers and interaction affordances out of exports. Acceptance includes an overlay-versus-source pixel comparison proving no app-drawn connecting segments, method/view transition checks, multiple independent sites, selected/unselected and locked/unlocked markers, and standard/narrow screenshots covering Result, Split, and Source in both Methods. Completed 2026-07-14. Evidence: fresh Cairo paths eliminate marker connections; six-view Voronoi/Threshold matrix verifies independent markers in Result/Split/Source only for Voronoi; standard/narrow locked and unlocked captures plus audit lock toggles verify marker states and feedback; exports remain overlay-free by architecture.
- [x] **THR-039** — `done` · P0 · Voronoi HSV — Investigate and correct the visibly wrong HSV Color matching output. The archived webapp classifies in a true three-dimensional HSV cylinder using `x = S·cos(H)`, `y = S·sin(H)`, and `z = V`; the native path currently uses a different ad hoc sum of circular Hue, Saturation, and Value deltas. Value is present in the native formula, so the observed failure must not be described as proven H/S-only binning, but the geometry is confirmed not to match the demonstrated engine and can materially change regions. Reproduce the supplied HSV/RGB/Source comparison with identical samples and Influence, isolate distance geometry from weighting, then restore the intended cylindrical 3D behavior or document and justify any deliberate deviation. Preserve circular Hue, stable behavior near zero Saturation, Value discrimination, deterministic ties, alpha handling, and floating-point precision. Acceptance requires archived/native reference assignments at hue seams, neutrals, equal-H/S differing-Value colors, differing-S/V colors, and representative image fixtures; inspected HSV/RGB/Source captures; full-resolution preview/export agreement; and a regression in the interactive audit. Completed 2026-07-14. Evidence: neutral-Influence archived geometry table covers 359/1 seam, neutrals by Value, equal H/S differing Value, differing S/V, cylinder discriminator, stable order independent of vector order, low alpha and transparent canonicalization; >1600px preview/full and PNG16 full-resolution/alpha agreement pass; Source/RGB/HSV captures were inspected. Influence formula is tested natively but no archived weight-parity claim is made. The archived-parity cylinder was later superseded by THR-057 after real-image testing exposed its near-black endpoint bug.
- [x] **THR-040** — `done` · P1 · Presets — Finalized human-readable preset JSON v2 as the only accepted user-preset format. It stores the complete intentional processing recipe, detaches image positions, excludes artwork/paths/export/UI/job state, and uses atomic XDG writes. Version 1 and malformed/incomplete files remain byte-for-byte untouched and appear as actionable diagnostics; no migration, rename, or automatic rewrite exists. Finalized 2026-07-16 with exact-version/missing-field/unknown-field, leak, round-trip, duplicate, atomic failure, and unchanged-file evidence; the user preset directory was hash-checked before and after this work.
- [x] **THR-041** — `done` · P1 · Voronoi site editing — Treat each site's Source center color as authoritative and editable, with its image-picker coordinate retained only as optional provenance/convenience. Each site owns its own Target color. Present separate Source and Target swatches on the same site row and open the transactional custom color picker for either; Target edits affect classification output only, while Source edits move the cell center and follow THR-042 reset behavior. Derive OKLab, encoded-sRGB, and cylindrical HSV coordinates from the site's one canonical Source color so changing Color matching never uses stale per-space coordinates. After manual Source editing, visibly detach or clear the old image position; resampling or moving a marker explicitly reattaches it. Persist Source, Target, optional position, Influence, and lock together per site. Acceptance includes transparent/low-alpha sampling policy, color-picker precision/transactions, matching-mode transitions, detach/reattach, project/preset round trips, deterministic preview/export agreement, and unambiguous Source versus Target labeling. Cross-reference THR-013, THR-033, THR-040, and THR-044. Completed 2026-07-14. Evidence: canonical Source with derived OKLab/encoded-RGB/HSV coordinates, separate transactional Source/Target edits, detached/attached marker state, v4 persistence, and focused core plus GTK evidence.
- [x] **THR-042** — `done` · P1 · Voronoi site replacement — Treat replacing a site's Source center as replacing that site's Source+Target pairing. A newly created site initially uses its sampled Source color as its Target. Thereafter, dragging, nudging, resampling, or directly color-editing that unlocked Source must always overwrite that same site's Target with the new Source color—even when the user previously customized the Target—and refresh the preview. Do not add follow/detach state or a confirmation: moving the center means abandoning the prior pairing and trying a new site+target. Merely selecting a site changes nothing. Make the reset immediately legible by updating both swatches during the completed adjustment; document undo belongs to THR-006, while THR-043 supplies deliberate protection. Acceptance includes create→move, nudge, resample, direct Source edit, customized Target overwrite, independent multi-site behavior, selection no-op, dirty/preview coalescing, project/preset round trips, and inspected Source/Result transitions. Cross-reference THR-038, THR-041, THR-043, THR-044, and THR-006. Completed 2026-07-14. Evidence: centralized create, drag, nudge, footprint, resample, and direct-Source replacement resets Target to Source; selection is a no-op. Expanded 2026-07-18: each replacement path now contributes one THR-006 document transaction; presets remain covered by THR-040.
- [x] **THR-043** — `done` · P1 · Voronoi locks — Give every site one visible lock protecting the complete Source-center+Target-color pair—the immutable site definition the creator has dialled in. A locked site remains selectable and inspectable but cannot be dragged, nudged, resampled, Source-edited, Target-edited, have its Influence changed, or be deleted until explicitly unlocked. Disable all relevant controls and give immediate explanatory feedback rather than silently ignoring input. Be mathematically honest: the lock freezes that site's own parameters, not its exact pixel-membership boundary, which can still move when another unlocked site or the global matching mode changes. New sites default unlocked; locking/unlocking alone does not change processing values or schedule a preview. Persist the lock with the site in projects and presets. Acceptance covers every pointer, keyboard, color-picker, Influence, and deletion path; accessible lock state; standard/narrow layouts; no mutation under lock; unlock→edit; global/neighbor changes without locked-parameter mutation; undo/dirty behavior; and project/preset round trips. Cross-reference THR-033, THR-041, THR-042, THR-044, THR-006, and THR-040. Completed 2026-07-14. Evidence: visible persisted site lock disables Source, Target, Influence, footprint and deletion; pointer/keyboard movement is rejected; lock toggles dirty state without scheduling preview. Expanded 2026-07-18: lock/unlock is undoable, while every refused locked mutation adds no history entry.
- [x] **THR-044** — `done` · P1 · Voronoi data model — Refactor the native Voronoi recipe around independent sites rather than `ColorGroup { output_color, samples[] }`. Each site is the atomic cell definition: stable ID/order, canonical Source centerpoint in the selected three-dimensional color space, its own Target color, optional competitive weight (currently exposed as Influence), lock, and optional source-image position/sample metadata. For every visible pixel, compute the selected metric against the complete site set, apply the defined weight rule when enabled, choose the nearest eligible site, and assign that site's Target; cell boundaries are therefore derived from surrounding centerpoints, their weights, and the global metric rather than stored as site data. Ties remain deterministic and alpha policy remains deliberate. Locking freezes a site's centerpoint, weight, and Target but never promises a fixed boundary while neighboring sites, weights, or the metric change. Do not retain Color groups as a hidden processing abstraction unless they become purely optional organization with no shared Target semantics. Migrate existing v3 groups by flattening every sample into a site that inherits the former group's Output and preserves its own Source/Influence/position; define empty-group handling and bump/version the project schema safely. Update auto-initialization, processing, coverage, selection, editing, presets, export, CLI evidence, and tests together so preview/full export cannot disagree. Acceptance includes one-site-one-cell fixtures, unweighted/default-weight geometry, controlled weighted-boundary shifts, surrounding-site edits, multiple former samples sharing an inherited Target but becoming independently editable afterward, migration round trips, stable IDs/ties, all matching modes including THR-039 cylindrical HSV, locks, and inspected standard/narrow workflows. Completed 2026-07-14. Evidence: independent VoronoiSite model, site-ID coverage, stable order/ID ties, preserved weighted distance and alpha policy, true HSV cylinder, deterministic v3 flatten migration, v4 validation, 55 core tests, strict build gates, audit, and inspected standard/narrow captures.
- [x] **THR-046** — `done` · P1 · Color pipeline — Audited the native and archived perceptual paths before implementation. Tagged RGB inputs transform from embedded ICC to canonical encoded sRGB in floating point with relative-colorimetric intent before linearization and OKLab; invalid/non-RGB profiles fail explicitly. The finalized current-only project v5 path always uses that managed decode, and THR-018 removed the former pre-release unmanaged reopen path. Perceptual matching remains exactly Cartesian OKLab `dL² + da² + db²` without axis normalization or UI-space distance. Automatic sites cluster an alpha-weighted bounded histogram of the complete source distribution and choose actual full-resolution Point samples, eliminating nearest-neighbor proxy phase loss. Influence scales the complete squared metric by `2^-clamp(Influence,-4,4)`, with its radial meaning documented. The current bounded linear-sRGB display/PNG/EXR policy is explicit; OKHSL UI and extended-gamut delivery remain correctly owned by THR-051/013/027/045. Completed 2026-07-15 and reconciled with the final format contract on 2026-07-16. Evidence: independent Display-P3 matrix fixture, tagged-sRGB/current-project tests, known OKLab colors/round trips, axis-discriminator and ±4 Influence assignments, checkerboard distribution regression, strict all-target Clippy, release build, 8/8 GTK audit, inspected standard Result and narrow Split artifacts with perceptual sidecars, and independent closeout review.
- [x] **THR-047** — `done` · P1 · Thresholds — Replaced the inspector's insensitive Link, Process/Bypass, and Bands widgets with synchronized plain-text summaries; **Working space** is the one editable inspector control and **Edit mapping…** is an explicit focusable launcher for detailed edits. State refresh remains authoritative after dialog transactions, presets, history, and space changes. Completed 2026-07-15. Evidence: the inspector ownership audit verifies one-preview RGB→HSV switching, synchronized Hue summary, focusable Working space and launcher, 91 native tests, strict Clippy/release gates, 8/8 GTK audit, and inspected 1180×760 plus 720×700 captures.
- [x] **THR-048** — `done` · P2 · Threshold dialog — Removed the redundant **Remove Band** and **Add Band** buttons. One compact 2–32 Bands numeric stepper now owns plus/minus changes while retaining exact safe split/merge behavior, selected-handle semantics, locks, linked propagation, local history, and one preview per completed change. Completed 2026-07-15. Evidence: focused split/merge model tests, live increase/decrease/undo/locked audit paths, and inspected standard/narrow dialog captures.
- [x] **THR-049** — `done` · P2 · Threshold dialog — Removed **Working space** from **Edit Threshold Mapping** and made the inspector selector authoritative. A newly opened dialog inherits the active RGB or HSV space; the modal parent lock plus an explicit live-dialog guard prevents ambiguous external space changes, while retained per-space mappings and dialog Done/Cancel behavior remain intact. Completed 2026-07-15. Evidence: inspector switch→HSV dialog reopen→Hue edit, global dialog undo, whole-state Cancel, focus/accessibility audit, and inspected standard/narrow captures.
- [x] **THR-050** — `done` · P2 · Threshold dialog — **Synchronize mappings** continues to name its source and every destination, now striking through each locked destination immediately. Its non-visual accessible label and description explicitly name writable and locked-skipped channels. Hue remains excluded and label refresh does not schedule processing. Completed 2026-07-15. Evidence: zero/one/two locked RGB and locked Saturation→Value presentation fixtures, existing synchronization/lock model tests, live GTK lock-change markup assertion, one-preview copy assertion, strict gates, and inspected narrow layout.
- [x] **THR-053** — `done` · P1 · Voronoi inspector — Consolidated site browsing and editing into one **Color sites** section. Every row directly exposes labeled Source and Target swatches, lock, Influence, attachment, and coverage; either swatch opens the transactional picker and selected-site precision actions remain directly beneath the list. Removed the redundant Add Site action: clicking empty visible Voronoi artwork now samples and creates a site, while a 14-pixel marker hit selects the existing site and Threshold mode ignores the gesture. Moved Color matching directly below Smooth, made it Voronoi-only, placed Presets after active creative controls, and removed Hue Rotation from the main inspector in every mode. Completed 2026-07-15. Evidence: 97 native tests, strict all-target Clippy, optimized build, 8/8 GTK audit including Target-picker and direct-add paths, and inspected standard/narrow Voronoi plus Threshold artifacts.
- [x] **THR-054** — `done` · P1 · Desktop workspace — Rebuilt the inspector around compact, keyboard-collapsible desktop sections. Method now combines the engine toggle, auto-apply Presets and Voronoi Matching without explanatory-card overhead; Smooth lives beside the comparison controls; Color sites receives the bulk of sidebar height with compact master/detail rows; Pipeline/profile data occupies the footer's far right. Split manipulation is direct on the canvas through a visible divider and grip, with a 12-pixel drag target, cursor feedback, bounded geometry and keyboard fallback; the separate lower slider is gone and site interactions remain available away from the divider. Completed 2026-07-15. Evidence: 98 native tests, strict all-target Clippy, 8/8 GTK audit, optimized build, standard Result/Split, narrow Split and Threshold artifacts, and independent UX review.
- [x] **THR-055** — `done` · P1 · Voronoi site details — Replaced the detached global Site details panel and selection-dependent Influence row with one concise accordion tile per site. Each visible title is only its sequential number; aligned Source and Target swatch columns, attachment/coverage, lock, and disclosure remain in the primary row. Influence, Source position, Footprint, and Delete live inside the expanded tile, with zero-or-one open and an open panel following row/canvas selection by stable site ID. Removed the Marker/ID/coordinate block while retaining internal IDs for processing and persistence. Completed 2026-07-16. Evidence: 98 native tests, an explicit live accordion identity/single-open audit, 8/8 GTK scenarios with no critical diagnostics, and inspected 1180×760 plus 720×700 expanded-site captures.
- [x] **THR-056** — `done` · P1 · Voronoi color matching — Added explicit **Perceptual (OKHSL)** matching beside Perceptual (OKLab), RGB, and HSV. Bounded canonical Source/pixel colors compare in the seam-safe unit cylinder `(S·cos(2πH), S·sin(2πH), L)` with ordinary squared Euclidean distance; zero Saturation erases Hue, and Influence scales the complete metric without per-axis weights. OKLab remains the default. The UI and documentation deliberately describe this as perception-informed cylindrical matching rather than ΔE-grade uniformity. Completed 2026-07-16. Evidence: canonical coordinate, seam, neutral, Saturation, Lightness, Influence, tie, alpha, project/preset, legacy-HSV serialization, and preview/full fixtures; 100 native tests; strict Clippy; optimized build; 8/8 GTK audit visiting all four modes; inspected standard/narrow OKHSL artifacts with actual/asserted sidecars; and independent review with no P0/P1 findings. Directional release startup-to-screenshot timing on the embedded 1254×1254 example had a 1.60s median for OKHSL versus 1.36s for HSV across three runs; one OKHSL screenshot-retry outlier took 3.17s, so these numbers bound end-to-end interaction cost rather than isolate the matching kernel. THR-057 later changed only HSV processing geometry; THR-058 then removed OKHSL matching from the creator-facing menu while retaining the dormant implementation for possible research.
- [x] **THR-057** — `done` · P0 · Voronoi HSV — Replace the archived unit-cylinder HSV metric with an endpoint-aware cone because relative Saturation and unstable Hue retained full radial weight near black, overwhelming a much smaller Value difference and sending dark pixels to brighter sites. HSV now compares `(S·V·cos(H), S·V·sin(H), V)`, which is equivalent to using encoded-RGB chroma as the radial coordinate and naturally collapses Hue at black. OKHSL processing is deliberately unchanged while real-image comparison determines whether that separate matching mode remains useful. Project and preset serialization remain compatible, but existing HSV cell boundaries intentionally change. Completed 2026-07-16. Evidence: the sampled `#010101` → `#010322` versus `#509bb4` regression now selects the darkest site; seam, neutral, Value, deterministic tie, Influence, alpha, preview/full and PNG16 export coverage remain green; 100 native tests, strict Clippy, optimized build, and an inspected live HSV render pass with the darkest example site increasing to 24.6% coverage.
## THR-060 - Voronoi-only GTK4 refactor

Status: implementation edits present; final verification and acceptance pending.

User-directed scope: remove Thresholds and mode switching; remove libadwaita;
adopt Toniator-style GTK4 presentation; extract authoritative document/session,
job, picker, and processing boundaries; preserve retained creative capabilities.
Project v6 and preset v3 deliberately reject older files without rewriting them.

The architecture checkpoint passed 88 tests before the final GTK4 edits.
Resource/template/dialog compilation, final regression and native UI checks,
and measured performance decisions remain unverified. See
`docs/FUNCTIONAL_AUDIT.md` for evidence and remaining checks. No user acceptance
or complete-refactor claim is implied.

Thresholds-only THR-998, THR-024, and THR-023 are superseded by the explicitly
requested feature removal, not fixed in their former implementation. Their
historical entries remain intact. Other issues remain open unless separately
resolved with evidence; this refactor does not imply blanket closure.

## THR-061 - Voronoi boundary blending / color transition

Status: implementation present; verification in progress, not complete or accepted.
Priority: P1.

User-approved direction: one global Transition width, Oklab/Linear RGB mixing,
Multisite/Shared borders scope, and smooth multiway junctions. Hard mapping and
coverage remain unchanged at width zero. Shared borders requires actual weighted
region contacts, not a nearest-two shortcut. Existing current-format files remain
readable; no legacy preset conversion is included.

Initial implementation passes 105 tests, strict Clippy and the app build. One new
whole-project round-trip test fails on existing f64 site-position JSON precision.
Rendered evidence also exposes an overly conservative coplanar contact fallback;
its correction is identified but pending approval. See
`docs/TRANSITION_VERIFICATION.md` for evidence and remaining work.
## THR-062 - Existing-project Save holds a read borrow while starting a job

Status: fixed and verified; existing-path Save completed in private Sway on 2026-09-19. See `docs/BUG_HUNT_FOLLOWUP_2026-09-19.md`.
Priority: P1.

In `src/shell_actions.rs`, `prompt_project_save` reads `project_path` in an
if-let condition and calls `start_project_save` from its body. The temporary
RefCell read guard survives into a path requiring mutable state in `begin_job`.
Copy the path to a local before the conditional, then exercise Save on an
already-named project. Not yet reproduced through a native file workflow.

## THR-063 - Drag completion does not enable document Undo

Status: fixed and verified; canvas drag enables Undo, with native Undo/Redo and savepoint readback. See `docs/BUG_HUNT_FOLLOWUP_2026-09-19.md`.
Priority: P1.

After moving a source marker in a clean Spectrum Example, pixels, coverage, and
dirty state update, but Undo stays disabled. `canvas_sampling` completes the
session gesture and schedules preview without synchronizing document history
controls. The session-level drag regression passes, demonstrating the missing
GTK projection coverage. See `docs/BUG_HUNT_2026-09-13.md` and `drag-result.png`.

## THR-064 - Picker local Undo does not restore expected displayed Lightness

Status: fixed and verified; rounded picker display input no longer adds spurious local edits. See `docs/BUG_HUNT_FOLLOWUP_2026-09-19.md`.
Priority: P2.

In Choose Target Color, set Lightness from approximately 1.7168 to 45. Activating
local Undo and reading Lightness still returned 45; Redo also returned 45.
Cancel and reopening returned 1.7168, so the tested draft did not leak into the
document. Diagnose gesture coalescing, rounding/no-op steps, and widget refresh
before choosing a fix. Do not infer global history failure from this result.

## THR-065 - Enforce application layout minimums and right-pinned side panel

Status: open; deferred, low-priority follow-up. The user accepts the current work with this known limitation; it is not an acceptance blocker.
Priority: P3 (low).

This is an application layout enforcement issue, not merely inspector clipping.
The inspector is the side panel. With it visible, enforce its minimum width and
keep it pinned to the right window border. The overall window minimum width
should accommodate the minimum canvas width plus the minimum side-panel width,
including the divider and relevant spacing. With the panel hidden, allow a
smaller window consistent with the canvas minimum. Preserve vertical scrolling
for settings that exceed the available height.

The user normally works with a larger window to view artwork and access settings;
the current small-window behavior is understandable and is not bothering them.
No layout implementation change is requested now.

Evidence: resizing the private output from 1440x1000 to 1024x600 leaves side-panel
controls clipped beyond the right edge. The attempted sizing fix did not resolve
this in the 2026-09-19 native check. Manual panel hiding yielded a usable 720x600
canvas in the earlier check. See `docs/BUG_HUNT_FOLLOWUP_2026-09-19.md` and
`responsive-1024.png` / `responsive-720-hidden.png` in the earlier bug-hunt evidence.
> THR-061 status update (2026-09-19): implemented and verification complete;
> artistic user acceptance pending. Priority P1. This supersedes the earlier
> implementation-in-progress and solver/serialization blocker notes below.
> Evidence: 111 tests, strict Clippy and build; three opt-in release checks;
> twelve inspected fixtures; native width, blend-space/scope, Undo/Redo,
> save/reopen and PNG 16-bit export checks. The dropdown harness failure is
> corrected and verified. See `docs/TRANSITION_SOLVER_CORRECTION.md` for paths,
> limits, and the measured high-site-count Shared borders cost. THR-065 remains
> deferred; no layout or user-owned CSS changes were made for this closeout.
## THR-066 - Pre-alpha file-format and preset cleanup

- Status: open; required before alpha, not part of the current mode removal.
- Priority: P1, pre-alpha readiness.
- Context: the user confirmed that file formats are not final and compatibility
  breaks are acceptable during this pre-alpha development phase.
- Scope: reconcile project/preset schemas with the final supported controls,
  refresh bundled fixtures and examples, inventory obsolete personal presets,
  and document the supported format and rejection policy before alpha.
- Preserve user-owned files; inventorying obsolete files is not permission to
  rewrite or delete them. Agree on cleanup handling before changing user data.
- Completion evidence: current-format round trips and rejection tests, valid
  bundled presets/examples, and an explicit documented alpha format decision.

> THR-061 follow-up: Shared borders, its scope control/state, and its contact
> solver were removed by user decision. Multisite blending is now the only
> behavior. No compatibility shim was added. The user accepted this work. Test
> cleanup and verification are complete: 103 tests, strict Clippy, build, three
> opt-in release checks, inspected images and a focused private-Sway transition
> check pass. See `docs/TRANSITION_TEST_CLEANUP.md`; earlier records below are
> historical. THR-066 remains open for pre-alpha format/preset cleanup.
